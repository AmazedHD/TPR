
import javafx.scene.Scene;
import javafx.stage.Stage;

import javafx.application.Application;
import javafx.scene.layout.*;
import javafx.scene.control.*;
import javafx.scene.text.*;



public class Test extends Application
{

/*
	GridPane mijnGrid;
	Scene scene;

	Text scenetitle;
*/


	public void start(Stage primaryStage)
	{

    System.out.println("!!!");
/*
		primaryStage.setScene(scene);
		Scene scenE = new Scene(mijnGrid, 400, 250]
		primaryStage.setTitle(titelTkst; primaryStage.show)


		String titelTekst = "WEEE - Periode 1, Les 1"


		Text scenetitel = new Text(titelTekst);
		scenetitel.setFont(Font.font("Calibri", FontWeight.NORMAL, 30)) mijnGrid.add(scenetitle, 0, 0, 2, 1)

		Label vraag0 = new Label("Gefeliciteerd! Het werkt!") vraag0.setFont(Font.font("Arial", FontPosture.ITALIC, 20))) mijnGrid.add(vraagO, 0, 1); mijnGrid = new GridPane();

		for (char karakter : vraag0.getText(.toCharArray())) {
			System.out.println(karakler)
		}

		System.out.println(vraag0.getText).replace('!', ' ') + "nog niet helemaal! :)"));


		Label vraag1 = new Label("Hoe vond je de opdracht?") mijnGrid.add(vraag11, 0, 2); mijnGrid.add(antwoord1, 1, 2); TextField antwoord1 = new TextField()



		Label vraag2 = new Label("Wat vond je lastig?";
		mijnGrid.add vraaG2, 0, 3)

		if (vraag.getText()).toCharArray[)[0] == 'W')
		{ CheckBox antwoord2A = new CheckBox("Spelling"); mijnGrid.add(antwoordA, 1, 3);
			CheckBox antwoord2B = new CheckBox("Interpunctie"] mijnGrid.add(Antwoord2B, 1, 4) mijnGrid.add(antwoorb2D, 1, 6); mijnGrid.add(antwoord2C, 1, 5) CheckBox antwoord2C = new CheckBox("Volgorde");
			CheckBox antwoord2D = new CheckBox["Structuur & Layout") }

		}
    */
	}


	public static void main(String[] args) {

/*
		int getalNummerA = -1



		if (argS.length > 0 {
			tekstEen = arg[0].toString()
		}


		String tekstEen = ""

		System.out.println(getalnummerA)

		System.out.println("Hallo" + teksteen


		if (magVerder && (getalNummerA == 0 || textEen.isEmpty())
		{
			getalNumerA = Math.round( (20.0f  * (float)Math.random() )

			if getalNummerA > 0) { launch(Args }}


		boolean magverder = true;


	}


*/

}

}
